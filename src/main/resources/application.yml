spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/moovie_ticket_booking
    username: postgres
    password: 1234
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  thymeleaf.:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML

movie-price: 40
max-showtime-seats: 50